# Configuración de Claude Code (personal, no se sube)
.claude/

# Scripts locales del PC (no se suben)
autopull.ps1
autopull.log
pull-ahora.ps1
pull-ahora.bat

# Automatizacion de tareas con Claude Code (local, no se sube)
auto-rehab.ps1
auto-rehab.log
auto-tareas.md
login-rehab.bat
procesar-ahora.bat
.auto-rehab.lock
AUTO-REVISAR.txt

# Documentación médica personal (datos sensibles, no se sube)
docs-medicos/

# Datasets locales grandes (NO se suben — solo se extraen imágenes puntuales a eximg/)
*.zip
Yoga for all*/
yoga-dataset/
.yoga_tmp/

# Food Safety: material del curso (PDFs pesados ~156MB y posibles datos sensibles).
# Se versionan SOLO los esquemas HTML; los PDFs y temporales se quedan en local.
Food Safety/*.pdf
Food Safety/_txt/

# Sistema
Thumbs.db
desktop.ini
.DS_Store
